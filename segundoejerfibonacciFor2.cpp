#include <iostream>
#include <vector> 
using namespace std;

// Funcion que calcula la puntuacion total optimizada 

int calcularpuntuacion ( conts  ""vector" < int > & numeros ) {
	int puntuacion = 0;
	
	for (int numero : numeros ) {
		puntuacion ++; //Regla 1: Agregar 1 punto por cada numero
		
		// Regla 2 y 3: usar condiciones combinadas 
		if (numero == 5) {
			puntuacio += 5; //Regla 3
		} else if (numero % 2 !=) {
			puntuacion += 3; //Regla 2 
		}
	}
    return puntuacion;     
	 }
	
int (main ) {
	vectores <int > aporte = {1, 2, 3, 4, 5, 17, 19, 21, 5, 5, 5,}; ejemplo Grande 
	 cout << "produccion diferente : " << calcular puntuacio (aporte ) << end ;
	 
	 return 0;

}	 
	


